server.port=8080

# ── Oracle — kết nối đến host machine từ container ───────────────────────────
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@//host.docker.internal:1521/AUTHPDB1}
spring.datasource.username=${DB_USERNAME:auth_user1}
spring.datasource.password=${DB_PASSWORD:Auth123456}

# ── Kafka — broker names trong docker internal network ────────────────────────
kafka.bootstrap-servers=${KAFKA_SERVERS:kafka-1:9092,kafka-2:9092,kafka-3:9092}

# ── Mail ──────────────────────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

spring.mail.username=${MAIL_USERNAME:cuongdaynemano@gmail.com}
spring.mail.password=${MAIL_PASSWORD:etpaliiporvypuev}

# ── Notify config ─────────────────────────────────────────────────────────────
notify.mail.from=${MAIL_FROM:noreply@iam-banking.vn}
notify.mail.portal-url=${PORTAL_URL:http://localhost:4200}
